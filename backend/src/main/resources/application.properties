# -------------------------
# Application Name
# -------------------------
spring.application.name=backend

# -------------------------
# Datasource / MySQL
# -------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/devops_phase2?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Bangkok
spring.datasource.username=root
spring.datasource.password=Id_6687001
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# -------------------------
# JPA / Hibernate
# -------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# -------------------------
# Session Config
# -------------------------
server.servlet.session.timeout=30m

# -------------------------
# Cookie Settings for cross-domain
# -------------------------
# สำคัญสำหรับ frontend <> backend อยู่คนละ subdomain
server.servlet.session.cookie.name=JSESSIONID
server.servlet.session.cookie.secure=true       # ต้องใช้ HTTPS
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=None    # สำคัญ: ส่ง cookie ข้าม subdomain
